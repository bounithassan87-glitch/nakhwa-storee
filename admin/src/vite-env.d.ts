/// <reference types="vite/client" />

// Injected at build time by Vite `define` (admin/vite.config.ts): the router
// basename — "/" in dev, "/admin" in the production build.
declare const __ADMIN_BASENAME__: string;
