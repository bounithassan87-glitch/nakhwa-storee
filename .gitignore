# === Large media (exceeds GitHub's 100 MB file limit) ===
# 4K masters. The optimized web renditions ARE committed and are what the
# site serves — see README for how they are produced.
#   0722.mp4        → old hero master (149 MB)
#   2026-master.mp4 → current hero master (155 MB, 2160x3840) → 2026.mp4
0722.mp4
2026-master.mp4

# Build archives / artifacts
dist.zip
*.zip

# Dependencies
node_modules/

# Editor / tooling
.claude/
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs / env / secrets
*.log
.env
.env.*
.dev.vars

# Backend / tooling
.data/
.wrangler/
prisma/*.db
